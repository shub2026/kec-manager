- 使用 `docker-compose.yml` 定义 `server`（Node.js/Express）与 `client`（Nginx/Vue）服务，通过自定义桥接网络 `kec-network` 实现内部通信。
- 前端服务配置 `depends_on` 等待后端健康检查通过后启动，确保服务依赖顺序。
- 根目录 `package.json` 利用 `concurrently` 提供一键启动前后端开发环境的统一入口 (`npm run dev`)。
- 共享 `.env` 环境变量配置（如 JWT 密钥、端口映射），由 Docker Compose 注入各子模块容器。