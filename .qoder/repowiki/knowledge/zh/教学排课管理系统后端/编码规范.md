- 采用 Controller-Service 分层架构，控制器负责请求解析与响应，服务层封装核心业务逻辑。
- 使用 `express-validator` 定义集中式验证规则，并在控制器中通过中间件形式注入执行。
- 全局实施命名自动转换，请求体从 camelCase 转为 snake_case 适配数据库，响应则反向转换适配前端。
- 统一使用 `winston` 记录结构化日志，涵盖请求追踪、业务警告及系统异常。