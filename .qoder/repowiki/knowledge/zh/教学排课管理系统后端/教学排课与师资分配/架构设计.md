- 采用标准的三层架构：`routes` 定义 RESTful 接口并挂载权限与校验中间件，`controllers` 处理请求参数与审计日志，`services` 封装核心业务逻辑。
- 服务层进一步细分为 `arrange` 子模块，将复杂算法（如 `auto-arrange.js` 中的评分模型）、数据查询（`queries.js`）与批量调度（`batch.js`）解耦。
- 依赖 Prisma ORM 进行数据持久化，通过 `audit.service.js` 记录操作轨迹，并利用内存级并发锁（`Set`）防止同一课程或学期的重复排课冲突。