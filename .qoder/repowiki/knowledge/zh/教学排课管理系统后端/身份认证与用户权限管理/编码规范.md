- 所有路由处理器均采用 try-catch 包裹并调用 next(error) 将异常移交全局错误处理中间件。
- 敏感操作（如登录失败、密码修改、用户状态变更）必须同步调用 createAuditLog 记录审计轨迹。
- 涉及用户输入的路由均挂载 sanitizeBody 或 validate* 中间件，强制执行 XSS 防护与参数校验。
- Service 层方法统一使用 static 关键字定义，并通过 prisma 客户端直接执行数据库事务操作。