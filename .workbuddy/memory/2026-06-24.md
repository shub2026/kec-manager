# 2026-06-24 工作日志

## 前端代码全面审查 + 修复

对 `client/src` 目录下所有核心模块完成审查，主要发现：

### 已确认良好的地方
- vite.config.js build.target=es2022（之前历史bug已修复）
- auth store initAuth 有 try-catch 兜底（历史修复）
- request.js 401 刷新有防循环机制（skipRefreshEndpoints + _retry 标记）
- main.js 图标按需注册（36个，非全量280+）
- router 守卫有完整 try-catch + 权限多级判断
- cache.js LRU 淘汰 + 定时清理
- cookies.js HTTPS 环境下自动加 Secure 标志
- 无 v-html 使用（XSS 零风险）
- 无 TODO/FIXME 遗留标记

### 修复完成项
1. **P1 ✅**: main.js `errorHandler` console.error 加 DEV 守卫（其余文件经核查均已守卫）
2. **P2 ✅**: `useCrudList.js` handleSave 加 catch + ElMessage.error 用户提示
3. **P2 ✅**: `stores/settings.js` load() console.error 加 DEV 守卫
4. **P2 ✅**: `composables/useSortable.js` 两处 console.error 守卫格式统一
5. **P2 ✅**: `composables/useExport.js` 两处 console.error 守卫格式统一
6. **P3 ✅**: `utils/download.js` downloadBlob 加 try-finally 确保节点和 URL 清理
7. **待办（非代码问题）**: TeachingArrange.vue 1609行，建议后续拆分子组件

## 全盘代码审查（后端+前端+排课算法+数据库）

3 个 Explore agent 并行扫描后端架构/排课算法/安全/API/数据库层，发现 33 个问题：
- **CRITICAL(4)**: 改密码被 XSS 清洗致永久锁死 / Refresh Token 不可撤销 / 系统重置清空审计日志 / trySwapUnassigned 缺意向约束
- **HIGH(7)**: .bak 备份文件残留 / 密码策略不一致 / 无账号锁定 / sanitizeBody 未全局 / querySemester 分页无上限 / 并发锁单进程 / 用户状态缓存 TOCTOU
- **MEDIUM(13)**: 路由内无 auth 兜底 / updateClass 级联删非事务 / 审计日志静默吞错 / 批量排课非原子 / 导入 N+1 / CSP 禁用 / 重置无速率限制 / 控制器直接持 Prisma 等
- **LOW(9)**: 健康检查暴露 DB / 下载令牌经 URL / 错误处理不统一 / 死代码 / trust proxy 等

完整报告写入 `overview.md`

## 优先项批量修复（8 项）

按 overview.md 修复计划执行，完成 8 项修复，全部语法验证通过：

1. **C-1 ✅** `xss.js` 新增 SKIP_SANITIZE_KEYS 白名单，密码字段跳过 XSS 清洗（修复永久锁死 bug）
2. **H-1 ✅** 删除 `teaching-arrange.service.js.bak-*`，.gitignore 加 `*.bak*` 规则
3. **H-2 ✅** `validation.js` validateChangePassword 正则改为与 validateUser 一致的严格字符集
4. **H-4 ✅** `app.js` 全局挂载 sanitizeBody（密码字段自动跳过）
5. **H-5 ✅** `query.controller.js` querySemester 分页强制上限 100
6. **M-2 ✅** `class.controller.js` updateClass 级联删除排课记录包入 $transaction
7. **M-7 ✅** `settings.routes.js` 所有 /reset/* 路由加 resetLimiter（每用户每小时3次）
8. **M-8 ✅** `pagination.js` validatePagination 接受 maxPageSize 参数

待办（需较大改动）：C-2 Refresh Token jti / H-3 账号锁定 / C-3 审计只追加 / C-4 排课意向约束 / H-6 Redis 锁 / M-5 导入N+1 / M-11 MySQL 迁移
