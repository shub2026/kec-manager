# 2026-06-20 工作日志

## 排课算法优化（teaching-arrange.service.js）

### 修复的问题
1. **trySwapOne 语法错误**（第784行）：`assignmentsByTeacher.get(t.id) = ...` 改为 `assignmentsByTeacher.set(t.id, ...)`
2. **levelPairs 重复代码**：删除多余的 `return true; };`
3. **try/catch 块不匹配**：删除 service 层多余的 try/catch

### 排课算法深度优化
- **isTeacherEligible**：增加学院/层次严格偏好检查，有偏好的教师不会被分配到非匹配班级
- **buildTeacherConstraints**：新增 `inherentTextbookIds`（固有教材快照）和 `assignedTextbookIds`（本轮已分配教材 Set）
- **calcMatchScore**：重写评分权重 — 学院+5、层次+5、本轮已分配教材+6、固有教材+3
- **排课流程**：4阶段 → 6阶段，阶段1-3有偏好教师严格匹配，阶段4-5无偏好教师教材内聚，阶段6兜底
- **assignRound**：分配后更新 `assignedTextbookIds`
- **trySwapOne**：置换时更新 `assignedTextbookIds`

### 前端修复
- **main.js**：添加 `Fold`、`Expand` 图标到全局注册（Layout.vue 侧边栏折叠按钮）

### 生成的文档
- `docs/SCHEDULING_ALGORITHM_OPTIMIZATION.md`：排课算法优化完整文档

## 排课教材内聚度深度分析（teaching-arrange.service.js）

### 背景
用户反馈：排课后大量教师同时上二三本教材，教材内聚力度不够。在前述算法优化（6阶段、评分权重调整）基础上，内聚问题仍未根治。

### 定位的根因（三叠加）
1. **兜底推导过宽**（第 276-293、362-367 行）：无排课记录教师被赋予该课程所有培养方案所有学期教材并集 → isTextbookMatch 对新教师永远 true → phase1 教材筛选失效。这是核心病灶。
2. **阶段优先级错位**（第 614-630 行）：phase3（有偏好·无教材要求）先于 phase4（无偏好·有教材匹配）→ 偏好教师被迫接多教材班级，无偏好但教材匹配的教师闲置。
3. **教材集合单调累加**（第 578-583 行）：教师每分配一个班级，班级所有教材加入 textbookIds，只增不减；评分中 assignedTextbookIds +6 > inherentTextbookIds +3，初始随机分散后被 +6 锁定。

### 提出的修复方案（分三档）
- P0：① 收紧兜底推导（空集合/仅主教材）② 插入 phase2.5 教材内聚优先阶段（isNewTextbookZero）
- P1：③ calcMatchScore 引入内聚惩罚（新增教材每本 -2）④ 新增教材内聚度统计指标（textbookCohesionRate/avgTextbookPerTeacher/scatteredTeacherCount）
- P2：⑤ 批量排课按教材分组预处理 ⑥ 内聚权重可配置化（TEXTBOOK_COHESION 常量）

### 关键决策
- 兜底推导改为空集合而非删除，保留 phase4 兜底路径避免未分配率飙升
- 内聚惩罚系数 -2 起步可调，避免过重影响分配率
- 建议先上 P0 观察 1-2 周期再上 P1，利用 preview 模式回归验证

### 生成的文档
- `docs/TEXTBOOK_COHESION_ANALYSIS.md`：教材内聚度优化完整分析报告
- `overview.md`：本次分析概览

## 教材内聚度优化全套修复落地

### 实施的修复（6 项全部完成）
1. **修复1·收紧兜底推导**（service L362-367）：无排课记录教师教材从全量并集改为空集合，受 `TEXTBOOK_COHESION.FALLBACK_EMPTY` 控制
2. **修复2·插入 phase2.5 阶段**（service L663-672）：在 phase2 与 phase3 之间新增"偏好+本轮零新增教材"阶段，受 `COHESION_PHASE_ENABLED` 控制；新增 `isNewTextbookZero` 辅助函数
3. **修复3·内聚惩罚评分**（service calcMatchScore L401-448）：权重全部配置化，固有教材 3→4，新增"新增教材每本 -2"惩罚项
4. **修复4·内聚度统计指标**（service calcAllMatchRates）：新增 `textbookCohesionRate`/`avgTextbookPerTeacher`/`scatteredTeacherCount`/`involvedTeacherCount`，preview 模式自动透出
5. **修复5·批量教材分组预处理**（service assignRound L579-592）：班级排序加入教材签名次要键，教材相同的班级连续分配
6. **修复6·内聚权重配置化**（constants/index.js）：新增 `TEXTBOOK_COHESION` 常量，含 ENABLED/权重/阶段开关/兜底开关/分散阈值

### 前端配套
- `TeachingArrange.vue`：单课程排课结果弹窗新增"教材内聚度"区块（内聚率/人均教材数/分散教师数），`cohesionRateClass` 计算属性按 ≥70/≥40/其他 分级着色

### 验证
- 三个后端文件 `node --check` 全通过
- 新实例启动到 "Server running" 阶段才因端口冲突退出 → 所有 import 成功解析
- 所有新增引用点 grep 闭合，常量字段名与使用点一致
- 3001 端口跑的是旧实例，需用户重启后端加载新代码

### 关键决策
- 兜底改空集合而非删除，保留 phase4 兜底路径
- 内聚惩罚系数 -2 起步可调，避免过重影响分配率
- 所有修复受 `TEXTBOOK_COHESION.ENABLED` 总开关控制，可一键回退
- 统计指标仅在 preview 模式透出，非预览不强制计算（避免无 teacherConstraints 时报错）

### 改动文件清单
- `server/src/constants/index.js`：+20 行 TEXTBOOK_COHESION 常量
- `server/src/services/teaching-arrange.service.js`：6 处修复，约 +120 行
- `client/src/views/teaching/TeachingArrange.vue`：+内聚度展示区块 + computed + 样式

## 首页 Dashboard 介绍部分优化

### 背景
用户反馈：项目做了大量功能新增和改动，但首页介绍部分内容单薄（仅 4 个功能卡片），与目标"使页面总体内容更饱满"不符。

### 改动内容（client/src/views/Dashboard.vue）
完全重构介绍卡片（intro-card），从原来的 4 卡片扩展为完整平台全景展示：

1. **Hero 头部**：KEC Logo + 平台名称 + 标语 + 版本标签 + 技术栈展示
2. **功能全景（6 大模块卡片）**：
   - 基础数据管理（学院/专业/层次/班级）
   - 课程与教材管理（课程目录/教材/批量导入/导出）
   - 培养方案管理（课程矩阵/多版本/课时分配/教材关联）
   - 教师与智能排课（教师档案/排课偏好/自动排课/手动调整/批量排课）
   - 查询与统计分析（开课查询/教材查询/课时统计/历史回溯）
   - 系统管理（用户管理/角色权限/审计日志/学期配置）
3. **核心亮点（6 项）**：智能排课算法、可视化课程矩阵、Excel 双向互通、完整审计追踪、灵活部署方案、安全权限体系
4. **底部信息**：版权、联系方式、技术栈

### 设计特点
- 渐变 Hero 区域（#f8faff → #f0f4ff → #faf8ff）
- 模块卡片 hover 动效（translateY + box-shadow）
- 技术栈 dot 色标（Vue 绿 / EP 蓝 / Node 绿 / Prisma 青 / SQLite 红 / Docker 蓝）
- 响应式三档断点（>900px / 600-900px / <600px）

### 验证
- Vite build（--mode development）编译通过，无错误
- 移除未使用的 `markRaw` 导入

## 系统重置 Bug 修复（settings.controller.js / DataReset.vue）

### 问题 1：系统重置未清空教师信息
**根因**：`resetSystem` 中 `classes` 先于 `teaching_assignments` 删除，SQLite + Prisma 事务内顺序错误导致子表删除失败。
**修复**：重排 `resetSystem` 内删除顺序——**先删所有子表/关联表**（`teaching_assignments`、`teacher_courses`、`teacher_scheduling_colleges`、`teacher_training_levels`、`plan_*`），**再删主表**（`teachers`、`classes`、…）。

### 问题 2：系统重置输入的操作原因未记入审计日志
**根因**：`resetSystem` 创建审计日志时 `details` 字段只写了 `{ type: 'system_reset' }`，未包含 `req.body.reason`。
**修复**：`details` 改为 `JSON.stringify({ type: 'system_reset', reason })`，并从 `req.body.reason` 取值。

### 修改文件
- `server/src/controllers/settings.controller.js`：`resetSystem` 函数（L278-316）重排删除顺序 + 引入 `reason`
- `client/src/views/settings/components/DataReset.vue`：系统重置说明文字补充"教师"

## 2026-06-20 15:25 系统设置页布局宽度调整

- 修改文件：`client/src/views/settings/SystemSettings.vue`
- 修改内容：将系统设置页的布局最大宽度从 `1200px` 调整为 `1400px`
- 原因：保持与首页概览（Dashboard.vue）的布局宽度一致
- 影响：系统设置页现在使用与首页相同的 `max-width: 1400px` 约束
