# 2026-06-21 工作日志

## 空状态提示统一（13:19）
- 参照方案查询页（`PlanQuery.vue`）已有的 `el-empty` 样式
- **教材查询页**（`UnifiedTextbookQuery.vue`）：将 `el-alert` 替换为 `el-empty`，两种状态（未选学期 / 未选教材）均使用空状态图
- **教学安排页**（`TeachingArrange.vue`）：在设置卡片后新增 `v-if="!selectedCourseId"` 的空状态卡片（`.empty-card`），文案"请选择课程查看教学安排"
