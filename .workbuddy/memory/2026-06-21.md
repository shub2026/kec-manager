# 2026-06-21 工作日志

## 空状态提示统一（13:19）
- 参照方案查询页（`PlanQuery.vue`）已有的 `el-empty` 样式
- **教材查询页**（`UnifiedTextbookQuery.vue`）：将 `el-alert` 替换为 `el-empty`，两种状态（未选学期 / 未选教材）均使用空状态图
- **教学安排页**（`TeachingArrange.vue`）：在设置卡片后新增空状态提示，去掉多余的 el-card 包裹

## 全站列表页空状态补全（13:30）
- 以下页面全部添加 `el-empty` 空状态（数据为空时显示）：
  - `TextbookList.vue` - 教材管理
  - `CourseList.vue` - 课程管理（同时新增按名称搜索筛选器）
  - `CollegeList.vue` - 学院管理
  - `MajorList.vue` - 专业类别管理
  - `TrainingLevelList.vue` - 培养层次管理
  - `TeacherList.vue` - 教师信息
  - `ClassTable.vue` - 班级管理（子组件）
  - `TeachingStatistics.vue` - 课时统计
  - `TeachingArrange.vue` - 教学安排
- 全局样式 `global.css` 已统一 `.card-header` 和 `.card-header-actions`（含 `flex-wrap: wrap`）
- commit `e983aee` 已推送

## 列表页闪烁修复 + 全站 UI 优化（13:50）
- **P0 修复**：所有列表页 `v-if="list.length > 0"` / `v-else` 改为 `el-table` 的 `#empty` 插槽，
  彻底消除分页/筛选切换时 DOM 销毁重建导致的闪烁
- **P0 优化**：教材查询页重置按钮增加 `:disabled` 状态
- **P1 优化**：全站 11 个页面标题增加 Element Plus 图标（main.js 补注册 `Avatar`）
- **P2 优化**：所有主要列表表格增加 `default-sort`（按名称 A-Z 排序）
- `global.css` 新增 `.filter-select-sm/md/lg/xl` 标准宽度类
- `.batch-operations` 新增 `margin-top: 16px`
- commit `f00f929` 已推送

## 开课查询页修复（13:50）
- **开课查询页**（`UnifiedSemesterQuery.vue`）：
  - 标题新增 `<DocumentChecked />` 图标
  - 未选学期时改用 `el-empty` 替代 `el-alert`（与其他页面统一）
  - 已选学期时保留统计信息 `el-alert`，并用 `<template v-else>` 包裹表格+分页
  - 重置按钮新增 `:disabled` 状态（无任何筛选条件时置灰）
- commit `1e94fd4` 已推送

## 教材查询页表格列宽固定（14:05）
- **问题**：切换教材时，因各教材信息长度不一致，表格列宽会跳动
- **根因**：`:table-layout="'fixed'"` 是错误的 Vue 写法，不会生效
- **修复**：
  - 去掉错误的 Vue 属性 `:table-layout="'fixed'"`
  - 新增 `.textbook-query-table` CSS 类，用 `table-layout: fixed` 强制固定表格布局
  - 给 `el-table` 标签添加 `class="textbook-query-table"`
- 切换教材时列宽不再跳动
- commit `81565e8` 已推送
