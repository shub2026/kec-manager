# 统一文本文件行尾符为 LF，避免 Windows CRLF 与 Prettier(endOfLine:lf) 冲突
* text=auto eol=lf

# 明确常见二进制类型，避免行尾符规范化损坏文件
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.xlsx binary
*.xls binary
*.pdf binary
*.db binary
*.sqlite binary
*.woff binary
*.woff2 binary
